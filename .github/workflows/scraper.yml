name: Scraper CAP
on:
  schedule:
    - cron: '0 6 * * 1,3,5'
  workflow_dispatch:
jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install psycopg2-
